

/*
Class  have intance which is called object 
Ex: User user1, user2, user3;

NOTE: struct and class in C++ is the same. but, we should use struct for smaller class
struct for define new data type from basic datatype
class for define both new datatype and method.

NOTE: struct and class in other language if different

Methods in class:

*/

/*
Lecture 82: 
struct and class

Access modifier
1. Private (by default in class) (accessed inside class definition, or through methods)
2. Public (by default in struct)(visited by object, ex: user1 in User user1;)
3. Protected (for inheritance)

variables/ data members, methods are commonly called members


NOTE: in struct, we should store data members or variables ONLY ******
*/